<form method="post" action="{{ path('repas_delete', {'id': repas.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce repas ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repas.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>