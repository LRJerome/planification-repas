{% extends 'base.html.twig' %}

{% block title %}Accueil - Planification de Repas{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .welcome-section {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            padding: 3rem 0;
            border-radius: 15px;
            margin: 2rem 0;
        }
        .feature-card {
            background: white;
            border-radius: 10px;
            padding: 1.5rem;
            margin-bottom: 1rem;
            box-shadow: 0 2px 15px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }
        .feature-card:hover {
            transform: translateY(-5px);
        }
        @media print {
            .no-print {
                display: none !important;
            }
        }
    </style>
{% endblock %}

{% block body %}
<div class="container">
    <div class="welcome-section text-center border border-black rounded-3 shadow">
        <h1 class="display-4 mb-4">Bienvenue dans votre planificateur de repas</h1>
        <p class="lead">Organisez vos repas, gérez vos recettes et générez vos listes de courses facilement.</p>
        
        <div class="mb-4 d-flex justify-content-center gap-3 no-print">
            <a href="{{ path('app_repas_new') }}" class="btn btn-success btn-lg">
                <i class="fa-solid fa-utensils"></i> Créer une recette
            </a>
            <a href="{{ path('planning_new') }}" class="btn btn-primary btn-lg">
                <i class="fa-solid fa-calendar-plus"></i> Ajouter au planning
            </a>
            </div>
    </div>

    <div class="row">
        <div class="col-md-4">
            <div class="feature-card">
                <h3><i class="fas fa-calendar-alt text-primary me-2"></i>Planning</h3>
                <p>Organisez vos repas pour la semaine et visualisez votre planning en un coup d'œil.</p>
                <a href="{{ path('planning_index') }}" class="btn btn-outline-primary">
                    Voir le planning
                </a>
            </div>
        </div>
        <div class="col-md-4">
            <div class="feature-card">
                <h3><i class="fas fa-book text-success me-2"></i>Recettes</h3>
                <p>Gérez votre collection de recettes et accédez-y facilement quand vous en avez besoin.</p>
                <a href="{{ path('index') }}" class="btn btn-outline-success">
                    Voir les recettes
                </a>
            </div>
        </div>
        <div class="col-md-4">
            <div class="feature-card">
                <h3><i class="fas fa-shopping-cart text-info me-2"></i>Courses</h3>
                <p>Générez automatiquement votre liste de courses basée sur votre planning.</p>
                <a href="{{ path('liste_courses_index') }}" class="btn btn-outline-info">
                    Liste de courses
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}